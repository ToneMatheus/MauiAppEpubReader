<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiAppEpubReader"
             x:Class="MauiAppEpubReader.EditPage"
             Title="Edit">
    <!--<VerticalStackLayout>
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <ScrollView>
            <local:CustomEditor AutoSize="TextChanges" Text="{Binding HtmlContent}" />
        </ScrollView>
        
    </VerticalStackLayout>-->
    <ScrollView>
        <Grid RowDefinitions="Auto, Auto, *" Padding="10,5">
            
            <Label Text="Text From the Book Page:"/>
            <local:CustomEditor AutoSize="TextChanges" 
                            Text="{Binding HtmlContent}" />
            
            <StackLayout Grid.Row="1" Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="End" Spacing="20">
                <Entry x:Name="inputView" Placeholder="Enter title here" Text="{Binding Title}" />
                <Button Text="Add" Command="{Binding AddCommand}" />
            </StackLayout>
        </Grid>
    </ScrollView>
</ContentPage>