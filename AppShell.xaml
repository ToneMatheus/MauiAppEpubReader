<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiAppEpubReader.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiAppEpubReader"
    Shell.FlyoutBehavior="Flyout"
    Title="MauiAppEpubReader">

    <!--<Shell.MenuBarItems>
        <MenuBarItem Text="File">
            <MenuFlyoutItem Text="Open" Command="{Binding OpenFileCommand}" />
            <MenuFlyoutSeparator />
            <MenuFlyoutItem Text="Exit" Command="{Binding CloseCommand}" />
        </MenuBarItem>
    </Shell.MenuBarItems>-->

    <!--<FlyoutItem Title="Open" FlyoutDisplayOptions="AsSingleItem">
        <ShellContent Title="Open" ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>-->
    <FlyoutItem Title="Home" FlyoutDisplayOptions="AsMultipleItems">
        <ShellContent
             Title="Home"
             ContentTemplate="{DataTemplate local:MainPage}"
             Route="MainPage" 
        />
        <ShellContent
            Title="Edit"
            ContentTemplate="{DataTemplate local:EditPage}"
            Route="EditPage" 
        />
        <ShellContent
            Title="Text Sprouts"
            ContentTemplate="{DataTemplate local:TextSproutPage}"
            Route="TextSproutPage" 
        />
    </FlyoutItem>

</Shell>
